using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DiamondSquareTest : MonoBehaviour
{

    public int[,] intArray;
    int tempSize = 3;
    int _size = 3;
    int tempLength;


    // Start is called before the first frame update
    void Start()
    {
        intArray = new int[3, 3];

        intArray[0, 0] = 3;
        intArray[0, 2] = 7;
        intArray[2, 0] = 6;
        intArray[2, 2] = 3;

        DiamondSquare(intArray, 3);
    }

    void DiamondSquare(int[,] intArray, int size)
    {
        tempLength = size / 2;
        if (tempLength < 1)
        {
            return;
        }

        //Square algorithm
        for (int y = tempLength; y < _size; y += size)
        {
            for (int x = tempLength; x < _size; x += size)
            {
                Square(intArray, )
            }
        }
    }

    void Square(int[,] intArray, int length)
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
Footer
